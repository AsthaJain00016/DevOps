
services:
  backend:
   build: ./Server
   container_name: backend
   restart: always
   env_file:
    - ./Server/.env
   ports:
    - "8020:8020"
  frontend:
    build: ./Client
    container_name: frontend
    restart: always
    ports:
     - "5173:5173"
    depends_on:
      - backend

